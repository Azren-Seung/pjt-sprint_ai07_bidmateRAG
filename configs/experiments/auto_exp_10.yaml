title: auto_exp_10
name: auto_exp_10
mode: full_rag
notes_path: configs/experiments/notes/example-budget-quality.yaml
provider_configs:
  - configs/providers/openai_gpt5mini.yaml
retrieval_top_k: 40
top_k: 40
rerank_k: 7
alpha: 0.4
prompt_template: fact_extraction_first
hypothesis:
  - 넓은 후보·중간 깊이 리랭크·낮은 alpha로 혼합 신호를 쓰면서 사실 추출 우선 생성으로 균형 잡힌 앙상블적 동작을 기대한다.
changes:
  - top_k 40, rerank_k 7, alpha 0.4, prompt_template fact_extraction_first (Ensemble)
  - 추론 모델용 — temperature 미사용
expected_outcome:
  - 전반 지표에서 단일 튜닝 대비 안정적 평균 성능
next_actions:
  - 01·05와의 ablation 비교
