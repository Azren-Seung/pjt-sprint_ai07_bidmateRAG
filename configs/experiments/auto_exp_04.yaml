title: auto_exp_04
hypothesis:
  - 메타데이터를 리랭커 입력에 포함하면 예산/기관 관련 근거 정렬 정확도가 높아진다.
changes:
  - LLM 컨텍스트 헤더에 사업 금액/공개연도/기관유형 추가
  - 다문서 비교 질문에서 기관별 retrieval merge 추가
  - 리랭커 입력을 query + metadata + context로 확장
expected_outcome:
  - 예산 질문 무응답 감소
  - 비교형 질문 retrieval hit 개선
next_actions:
  - judge 기준으로 실패 유형 재분류
failure_cases:
  - question_id: Q007
    why_watch: 다문서 예산 차액 계산
  - question_id: Q014
    why_watch: 다문서 합산 + 공통요소 추출
hyperparameters:
  reranker_input: "query + metadata + context"
  alpha: 0.5
